services:
  todo-app:
    build: .
    ports:
      - "2025:3000"
    environment:
      - DB_URL=mongodb://mongo:27017/todo
      - PORT=3000
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
